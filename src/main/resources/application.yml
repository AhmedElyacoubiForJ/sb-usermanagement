server:
  port: 9090
spring:
  datasource:
    url: jdbc:mysql://localhost:3306/end_to_end_db
    username: root
    password:
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
